apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: three-tier-cluster
  region: ap-south-1

managedNodeGroups:
  - name: linux-nodes
    instanceType: t3.micro
    desiredCapacity: 1
    minSize: 1
    maxSize: 1
